//编辑return修改按钮名称
function getButtonName() {
    // 在这里修改按钮名称
    return "按钮名称";
}

//按钮按下时，会执行executeScriptFunctions()函数，请在executeScriptFunctions()里面定义并调用你所有的函数
function executeScriptFunctions() {
    // 示例函数1：弹窗消息
    function sayHello() {
        alert("欢迎来到思源笔记"); // 在弹窗中显示内容
    }

    /* 示例函数2：自定义按钮css样式
    function overrideStyles() {
        var button = document.getElementById("myButton");
        button.classList.remove("button"); // 移除原有的class
        button.style.backgroundColor = "red"; // 修改背景颜色
        button.style.color = "black"; // 修改文字颜色
        // 在这里可以添加其他需要修改的样式
    } */

    // 示例函数3：创建新笔记本
    function createNotebook() {
        fetch('/api/notebook/createNotebook', {
            method: 'POST',
            headers: {
                // 自定义请求头(这里不需要)
            },
            body: JSON.stringify({
                "name": "新笔记本名字"
            })
        })
    }

    // 示例函数4：通过 Markdown 创建文档
    function ccreateDocWithMd() {
        fetch('/api/filetree/createDocWithMd', {
            method: 'POST',
            headers: {
                // 自定义请求头(这里不需要)
            },
            body: JSON.stringify({
                "notebook": "20210817205410-2kvfpfn",   /* 笔记本id */
                "path": "/foo/bar",   /* 创建路径 */
                "markdown": "# 你好你好你好你好你好你好"   /* md格式的文档内容 */
            })
        })
    }

    // 更多函数请阅读思源官方api文档：https://github.com/siyuan-note/siyuan/blob/master/API_zh_CN.md

    // 执行所有函数
    sayHello();
    createNotebook();
    ccreateDocWithMd();
}